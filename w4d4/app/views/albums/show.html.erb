<h1><%= @album.title %></h1>

<ul>
  <li>Recording Band: <%= @album.band.name %></li>
  <li>Album Type: <%= @album.album_type %></li>
</ul>

<p>
  <%= link_to "Add a track to #{@album.title}", new_album_track_url(@album) %>
</p>

<h2>TRACK LIST</h2>

<ul>
  <% @tracks.each do |track| %>
    <li><%= link_to track.song, track_url(track) %></li>
  <% end %>
</ul>

<p>
  <%= link_to "Edit this album", edit_album_url(@album) %>
</p>

<p>
  <%= button_to "DELETE this album", album_url(@album), method: :delete %>
</p>

<p>
  <%= link_to "<< Back to #{@album.band.name}", band_url(@album.band_id) %>
</p>